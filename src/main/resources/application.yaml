spring:
    application:
        name: calendula_api
    data:
        mongodb:
            uri: ${MONGO_URI:mongodb://root:root@localhost:27017/calendula}
            authentication-database: ${DB_NAME:calendula}
